import numpy as np

# Path for exported to the data as a numpy arrays
FILE_PATH_NAME =  'Data/MP_Data2' 

# Actions that we try to d  etect for create to the Data set
Actions = np.array(['learn'])

no_sequences= 30 # 30 videos worth of data
sequence_length = 30 # videos are going to be 30 frames in length
start_folder = 30 # Folder start

